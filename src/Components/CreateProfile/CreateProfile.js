import React, {Component} from 'react';
import {connect} from 'react-redux';
import {Link} from 'react-router-dom';

import './CreateProfile.css';

export default function CreateProfile() {
  return (
    <div>This is the CreateProfile page...</div>
  )
}